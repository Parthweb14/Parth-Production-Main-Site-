'use client';

export default function LiveProjectButton() {
  return (
    <div
      className="inline-block rounded-full border-2 border-amber-500 text-amber-500 font-semibold uppercase tracking-[0.2em] px-8 py-3 sm:px-10 sm:py-3.5 text-xs sm:text-sm select-none text-center cursor-default"
      style={{ fontFamily: 'var(--font-syne), sans-serif' }}
    >
      Live Project
    </div>
  );
}
